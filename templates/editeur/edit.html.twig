{% extends 'base.html.twig' %}

{% block title %}Edit Editeur{% endblock %}

{% block body %}
    <div class="panel panel-default">
        <div class="panel-heading">
            <h1>Edit Editeur</h1>
        </div>
        <div class="panel-body">

            {{ include('editeur/_form.html.twig', {'button_label': 'Update'}) }}
            <div style="display: flex;flex-direction: row;justify-content: flex-end">
            <a href="{{ path('editeur_index') }}"class="btn btn-primary"style="margin-right: 2px">back to list</a>
                {{ include('editeur/_delete_form.html.twig') }}
            </div>


        </div>
    </div>

{% endblock %}
